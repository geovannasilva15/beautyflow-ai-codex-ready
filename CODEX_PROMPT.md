# Prompt sugerido para Codex

Refatore e evolua o BeautyFlow AI preservando as funcionalidades existentes. Garanta que não existam pastas duplicadas e que o projeto rode com:

```bash
python -m uvicorn app.main:app --reload
python -m streamlit run frontend/streamlit_app.py
```

Melhore o visual, organize os módulos, evolua o Atendimento IA e prepare Campanhas para futura integração com WhatsApp real.
